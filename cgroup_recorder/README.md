# cgroup Recorder & parser

This is a tool that records various cgroup files with an interval set in the record script.

The parser is used for parsing and writing the recorded stuff to csv files for easier analysis.
